/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "dummyimage.com",
      "unsplash.com",
      "source.unsplash.com",
      "images.unsplash.com",
    ],
  },
};

export default nextConfig;
